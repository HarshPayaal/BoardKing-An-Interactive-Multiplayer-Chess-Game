<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BoardKing</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        
    .chessboard {
        box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.7);
      display: grid;
      grid-template-columns: repeat(8, minmax(0, 1fr));
      grid-template-rows: repeat(8, minmax(0, 1fr));
      width: 400px;
      height: 400px;
      transform: rotate(0deg);
      outline: 5px solid #000;
      outline-offset: 10px;
      border-radius: 10px; /* Adds rounded corners */
      box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.5); /* Adds a shadow for depth */
       }

  
        .piece.white {
            color: white !important;;
            filter: drop-shadow(0 0 2px rgba(0, 0, 0, 1));
         }
        
        .piece.black{
            color: black !important;;
        }

        .filpped {
            transform: rotate(180deg);
        }

        .square {
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .square.light {
    background: linear-gradient(to bottom right, #f0d9b5, #edd8a3);
}

.square.dark {
    background: linear-gradient(to bottom right, #b58863, #a57e5b);
}


        .piece {
           font-size: 40px; /* Increase the piece size slightly */
           cursor: pointer;
           text-shadow: 0px 2px 4px rgba(0, 0, 0, 0.7); /* Adds a shadow to pieces */
           transition: all 0.3s ease-in-out;
           cursor: grab;
           transition: transform 0.2s, box-shadow 0.2s;

          }

        .piece:hover {
           transform: scale(1.1);
           box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
          }

        .piece:active {
          cursor: grabbing;
          }  

        .piece.draggable {
               cursor: grab;
            }

        .piece.dragging {
              opacity: 0.8;
              box-shadow: 0 0 10px 5px rgba(255, 255, 0, 0.8); /* Glowing effect */
              }

        .flipped{
            transform: rotate(180deg);
        }
        
              /* Rotate piece for black player*/
        .flipped .piece {
              transform: rotate(180deg);
            }
            .captured-pieces {
            display: flex;
             gap: 10px;
              padding: 10px;
             }


    </style>
</head>
<body>
    <div class="captured-pieces white-pieces"></div>
    <div class="captured-pieces black-pieces"></div>

    <div class="w-full h-screen flex items-center justify-center" style="background-image: url('/background1.png'); background-size: cover; background-position: center;">
        <div class="chessboard w-96 h-96 bg-red-800"></div>
    </div>

    <script src="https://cdn.socket.io/4.7.5/socket.io.min.js" 
       integrity="sha384-2huaZvOR9iDzHqslqwpR87isEmrfxqyWOF7hr7BY6KG0+hVKLoEXMPUJw3ynWuhO" 
       crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js" 
       integrity="sha512-xRllwz2gdZciIB+AkEbeq+gVhX8VB8XsfqeFbUh+SzHlN96dEduwtTuVuc2u9EROlmW9+yhRlxjif66ORpsgVA==" 
       crossorigin="anonymous" referrerpolicy="no-referrer"></script> 
    <script src="/javascript/chessgame.js"></script>
</body>
</html>